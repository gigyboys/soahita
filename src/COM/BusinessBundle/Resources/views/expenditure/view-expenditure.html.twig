{% extends "COMBusinessBundle::business.html.twig" %}

{% block title %}
	Charge et dépense : {{ expenditure.name }}
{% endblock %}

{% block body %}
{{ include("COMPlatformBundle:platform:tooltip.html.twig") }}
 <section class="content-header">
      <h1>
        Charge et dépense : {{ expenditure.name }}
      </h1>
    </section>

    <section class="content container-fluid">

      <div class="row">
        <div class="col-md-12">
          <div class="nav-tabs-custom">
            <ul class="nav nav-tabs">
              <li class="active"><a href="#detail_tab" data-toggle="tab">Détails</a></li>
            </ul>
            <div class="tab-content">
                <div class="tab-pane active" id="detail_tab">
					<div>
						<table>
							<tr>
								<td style="padding:5px">Désignation<td>
								<td style="padding:5px">:<td>
								<td style="padding:5px">{{ expenditure.name }}<td>
							</tr>
							<tr>
								<td style="padding:5px">Montant<td>
								<td style="padding:5px">:<td>
								<td style="padding:5px">
									{% set amount=formatAmount(expenditure.amount) %}
									{{ amount }} Ar
								<td>
							</tr>
							<tr>
								<td style="padding:5px">Date<td>
								<td style="padding:5px">:<td>
								<td style="padding:5px">
									{{ expenditure.date  | date('d/m/Y')  }}
								<td>
							</tr>
							<tr>
								<td style="padding:5px">Description<td>
								<td style="padding:5px">:<td>
								<td style="padding:5px">
									{{ expenditure.description }}
								<td>
							</tr>
						</table>
						<div>
							<a href="{{ path('com_business_edit_expenditure', {'expenditure_id': expenditure.id}) }}" class="btn btn-flat btn-primary">Modifier</a>
						</div>
					</div>
					<div class="clear">
					</div>
                </div>
				
            </div>
          </div>
        </div>
      </div>
      
    </section>
	
{% endblock %}